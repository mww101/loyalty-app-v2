import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack{
            Image("Logo")
            Text("Welcome to Loyalty App")
                .padding()
        }
    }
}

#Preview {
    ContentView()
}
